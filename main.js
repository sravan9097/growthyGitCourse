function add (x,y){
    return x + y;
}

const subract = function (x,y){
    return x - y;
}

const multiply = (x,y) => x * y;

const divide = (x,y) => y === 0 ? 'undefined' : x/y;

function calculate (x,y){
    return output = divide(x,y);  
}

console.log(calculate(10,2));
